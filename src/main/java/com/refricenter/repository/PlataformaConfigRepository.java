package com.refricenter.repository;

import com.refricenter.model.PlataformaConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlataformaConfigRepository extends JpaRepository<PlataformaConfig, Long> {
}
